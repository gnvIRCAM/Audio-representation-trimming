include 'configs/models/heads/mlp.gin'

models.combine_fm_and_head:
    head = @models.MLP()

train.make_bce_loss:
    weight = 1

train.make_losses:
    task_loss = @train.make_bce_loss

train.Trainer.val_step:
    metrics = {
        'mAP': @metrics.compute_average_precision, 
        'AUROC': @metrics.compute_auroc
    }