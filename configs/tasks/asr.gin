include 'configs/models/heads/lstm.gin'

metrics.compute_wer:
    tokenizer = @data.Tokenizer()

models.combine_fm_and_head:
    head = @models.BLSTM()    

train.make_ctc_loss:
    weight = 1
    blank = 0
    pad_token = 1

train.make_losses:
    task_loss = @train.make_ctc_loss

train.Trainer.val_step:
    metrics = {
        'wer': @metrics.compute_wer, 
    }
