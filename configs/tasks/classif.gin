include 'configs/models/heads/mlp.gin'

models.combine_fm_and_head:
    head = @models.MLP()

train.make_cross_entropy_loss:
    weight = 1

train.make_losses:
    task_loss = @train.make_cross_entropy_loss

train.val_step:
    metrics = {
        'weighted_accuracy': @metrics.compute_weighted_accuracy, 
    }    